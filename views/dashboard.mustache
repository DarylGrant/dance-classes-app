<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!-- Link to Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Dance Fusion</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/courses">View Courses</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about.html">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Login</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-5">
        <h1 class="text-center text-white">{{title}}</h1>
        <p class="text-center mb-4">Welcome, <strong>{{organiser.username}}</strong>!</p>

        <!-- Dashboard Links Section -->
        <div class="row justify-content-center">
            <div class="col-md-6">
                <ul class="list-group">
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/courses/add" class="btn btn-secondary w-100">Add New Course</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/manage-courses" class="btn btn-secondary w-100">Manage Courses</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/add-organiser" class="btn btn-secondary w-100">Add New Organiser</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/organiser-list" class="btn btn-secondary w-100">Manage Organisers</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/remove-user" class="btn btn-secondary w-100">Remove User</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/generate-class-list" class="btn btn-secondary w-100">Generate Class List</a>
                    </li>
                    <li class="list-group-item bg-dark text-light text-center">
                        <a href="/auth/logout" class="btn btn-secondary w-100">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"></script>
</body>
</html>
